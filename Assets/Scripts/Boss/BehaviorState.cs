namespace VRJammies.Framework.Core.Boss
{
    public enum BehaviorState
    {
        Idle = 0,
        PlayerFound = 10,
        Attacking = 20,
        PlayerLost = 30,
        Stunned = 40,
    }
}